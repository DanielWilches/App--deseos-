import { FiltrolistPipe } from './filtrolist.pipe';

describe('FiltrolistPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrolistPipe();
    expect(pipe).toBeTruthy();
  });
});
